{% extends 'base.html.twig' %}

{% block title %}Prices{% endblock %}

{% block body %}
    <h1>Prices</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>PriceName</th>
                <td>{{ price.priceName }}</td>
            </tr>
            <tr>
                <th>PriceContent</th>
                <td>{{ price.priceContent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prices_index') }}">back to list</a>

    <a href="{{ path('prices_edit', {'id': price.id}) }}">edit</a>

    {{ include('prices/_delete_form.html.twig') }}
{% endblock %}
