<form method="post" action="{{ path('prices_delete', {'id': price.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ price.id) }}">
    <button class="btn">Delete</button>
</form>
