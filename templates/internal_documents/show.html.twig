{% extends 'base.html.twig' %}

{% block title %}InternalDocuments{% endblock %}

{% block body %}
    <h1>InternalDocuments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ internal_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ internal_document.name }}</td>
            </tr>
            <tr>
                <th>Document</th>
                <td>{{ internal_document.document }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('internal_documents_index') }}">back to list</a>

    <a href="{{ path('internal_documents_edit', {'id': internal_document.id}) }}">edit</a>

    {{ include('internal_documents/_delete_form.html.twig') }}
{% endblock %}
