<form method="post" action="{{ path('internal_documents_delete', {'id': internal_document.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ internal_document.id) }}">
    <button class="btn">Delete</button>
</form>
