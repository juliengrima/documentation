{% extends 'base.html.twig' %}

{% block title %}UserSociety{% endblock %}

{% block body %}
    <h1>UserSociety</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_society.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_society.Name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ user_society.Address }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ user_society.ZipCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ user_society.City }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_society_index') }}">back to list</a>

    <a href="{{ path('user_society_edit', {'id': user_society.id}) }}">edit</a>

    {{ include('user_society/_delete_form.html.twig') }}
{% endblock %}
