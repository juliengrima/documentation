<article class="box post">
    {{ form_start(form) }}
        <div id="editor">
        {{ form_widget(form) }}
        </div>
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</article>







